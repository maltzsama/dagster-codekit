# Default values for dagster-codekit.
replicaCount: 1

image:
  repository: ghcr.io/maltzsama/dagster-codekit
  pullPolicy: IfNotPresent
  tag: ""

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  annotations: {}
  name: ""

rbac:
  create: true

podAnnotations: {}
podSecurityContext:
  runAsNonRoot: true
  runAsUser: 1000
  fsGroup: 1000

securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities:
    drop:
      - ALL

service:
  type: ClusterIP
  httpPort: 8000
  grpcPort: 4000

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: codekit.local
      paths:
        - path: /
          pathType: Prefix
  tls: []

resources:
  requests:
    cpu: 250m
    memory: 256Mi
  limits:
    cpu: 1000m
    memory: 512Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80

nodeSelector: {}
tolerations: []
affinity: {}

# --- CodeKit Configuration ---

codekit:
  existingConfig: ""

  database:
    url: "sqlite:////data/codekit.db"

  launcher:
    mode: k8s
    k8s:
      namespace: dagster
      serviceAccount: ""
      imagePullPolicy: IfNotPresent
      ttlSecondsAfterFinished: 300
      forwardEnvVars:
        - DAGSTER_POSTGRES_USER
        - DAGSTER_POSTGRES_PASSWORD
        - DAGSTER_POSTGRES_DB
        - DAGSTER_POSTGRES_HOSTNAME
    docker:
      network: host
      autoRemove: true

  server:
    workers: 4

  auth:
    enabled: false
    # existingSecret sources the server's auth token from a Kubernetes Secret.
    # When set, the server reads the token at runtime via expandvars substitution
    # (${CODEKIT_AUTH_TOKENS} in the rendered config.yaml). Do NOT also set
    # `tokens` below — they are mutually exclusive (existingSecret wins).
    # The CLI client (snapshot/delete/rollback) needs its own CODEKIT_TOKEN env
    # var set separately, e.g. from the same Secret mapped into the CI pod.
    existingSecret: ""
    # existingSecretKey is the key inside the Secret referenced by existingSecret.
    existingSecretKey: "token"
    # tokens is the plaintext token list (ignored when existingSecret is set).
    tokens: []

  rateLimit:
    enabled: false
    requestsPerMinute: 60
    backend: memory
    redisUrl: ""

  logging:
    level: INFO
    format: console

  replicaCount: 0

# --- Persistence (for SQLite) ---
persistence:
  enabled: true
  size: 1Gi
  accessMode: ReadWriteOnce
